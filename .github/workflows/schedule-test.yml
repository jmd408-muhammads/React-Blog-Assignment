name: Schedule Test Pipeline

on:
  schedule:
    - cron: "35 9 * * *"   # Runs daily at 3:00 PM IST
  workflow_dispatch:        # Allows manual testing

jobs:
  test_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print test message
        run: echo "Scheduled pipeline executed at $(date -u) UTC"

